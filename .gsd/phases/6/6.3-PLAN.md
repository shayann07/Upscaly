---
phase: 6
plan: 3
wave: 2
---

# Plan 6.3: Refactor App.tsx Handlers & Quality Gate Verification

## Objective
Extract remaining complex handlers from `App.tsx` into custom hooks and execute full quality gate verification (`check:quality`), achieving zero ESLint warnings across the entire codebase.

## Context
- .gsd/SPEC.md
- .gsd/phases/6/RESEARCH.md
- src/App.tsx

## Tasks

<task type="auto">
  <name>Task 6.3.1: Extract useKeyboardShortcuts and refactor App.tsx handlers</name>
  <files>src/App.tsx, src/hooks/useKeyboardShortcuts.ts</files>
  <action>
    - Create `src/hooks/useKeyboardShortcuts.ts` to manage global keyboard shortcuts (Ctrl+O, Ctrl+S, Ctrl+H, Escape, Space).
    - Simplify job status handler complexity in `App.tsx` by extracting status update helper logic.
    - Ensure `App.tsx` function length is under 150 lines and cognitive complexity is under 20.
  </action>
  <verify>npm.cmd run check:ts && npm.cmd run lint:ts</verify>
  <done>App.tsx ESLint max-lines and complexity warnings completely eliminated.</done>
</task>

<task type="auto">
  <name>Task 6.3.2: Execute Quality Gate Verification</name>
  <files>src/**/*.{ts,tsx,css}</files>
  <action>
    - Run full quality gate validation: TypeScript check, Vitest unit test suite, ESLint zero-warnings check, Prettier formatting check, and Vite production bundle build.
  </action>
  <verify>npm.cmd run check:ts && npm.cmd run test && npm.cmd run lint:ts && npm.cmd run format:check:all && npm.cmd run build</verify>
  <done>Zero ESLint warnings achieved across all frontend files with 100% passing tests and clean production build.</done>
</task>

## Success Criteria
- [ ] `npm.cmd run lint:ts` passes with `--max-warnings 0` (0 warnings, 0 errors).
- [ ] `npm.cmd run check:ts` passes with 0 errors.
- [ ] `npm.cmd run test` passes all tests cleanly.
- [ ] `npm.cmd run build` succeeds cleanly.
