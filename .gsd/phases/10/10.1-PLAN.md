---
phase: 10
plan: 1
wave: 1
---

# Plan 10.1: Refactor NCNN Param Parser Pipeline & Add Unit Tests

## Objective
Split `parse_ncnn_param` into modular functions (`read_header`, `parse_layer_line`, `parse_layer_params`, `scale_factor_for_layer`) in `src-tauri/src/engine/param_parser.rs`, reducing cognitive complexity and adding comprehensive unit tests.

## Context
- .gsd/ROADMAP.md
- .gsd/phases/10/RESEARCH.md
- src-tauri/src/engine/param_parser.rs

## Tasks

<task type="auto">
  <name>Refactor NCNN param parser pipeline and add parser unit tests</name>
  <files>src-tauri/src/engine/param_parser.rs</files>
  <action>
    1. Refactor `parse_ncnn_param` in `src-tauri/src/engine/param_parser.rs` by extracting:
       - `read_header`: extracts magic header token, layer_count, blob_count.
       - `parse_layer_line`: processes tokens and extracts input channels and scale factors.
       - `parse_key_value`: parses `k=v` token.
       - `scale_factor_for_layer`: inspects layer type (`Input`, `Interp`, `Deconvolution`/`Deconv`, `PixelShuffle`/`Subpixel`).
    2. Eliminate float casting clippy warnings (`scale_val.round() as u32`).
    3. Add unit tests for:
       - Header line parsing (empty header, valid magic token)
       - Interp layer scale math
       - Deconvolution depthwise stride math
       - PixelShuffle subpixel math
       - Fallback scale behavior when no scale layer exists
  </action>
  <verify>cargo test --manifest-path src-tauri/Cargo.toml</verify>
  <done>NCNN param parser refactored; cognitive complexity reduced; unit tests passing.</done>
</task>

## Success Criteria
- [ ] `cargo test --manifest-path src-tauri/Cargo.toml` passes with new param parser unit tests
- [ ] Function cognitive complexity for `parse_ncnn_param` is <= 15
