---
phase: 7
plan: 2
wave: 1
---

# Plan 7.2: Extract Model, Files, and Diagnostic Commands

## Objective
Extract model management, file launcher, window control, and system diagnostic commands from `src-tauri/src/lib.rs` into `commands/models.rs`, `commands/files.rs`, and `commands/diagnostics.rs`.

## Context
- .gsd/ROADMAP.md
- .gsd/phases/7/RESEARCH.md
- src-tauri/src/lib.rs
- src-tauri/src/model_manager.rs
- src-tauri/src/video_pipeline.rs

## Tasks

<task type="auto">
  <name>Extract Model, Files, and Diagnostic command submodules</name>
  <files>src-tauri/src/commands/models.rs, src-tauri/src/commands/files.rs, src-tauri/src/commands/diagnostics.rs, src-tauri/src/commands/mod.rs, src-tauri/src/lib.rs</files>
  <action>
    1. Create `src-tauri/src/commands/models.rs` with `get_installed_models`, `list_installed_models`, `list_available_models`, `get_model_catalog`, `check_for_model_updates`, `download_model`, `download_model_files`, and `repair_model`.
    2. Create `src-tauri/src/commands/files.rs` with `check_file_exists`, `open_file_native`, `show_in_explorer_native`, `close_window`, `minimize_window`, and `toggle_maximize_window`.
    3. Create `src-tauri/src/commands/diagnostics.rs` with `get_system_diagnostics` and `SystemDiagnostics`.
    4. Register submodules in `src-tauri/src/commands/mod.rs` and update `lib.rs`.
  </action>
  <verify>cargo test --manifest-path src-tauri/Cargo.toml</verify>
  <done>Models, Files, and Diagnostic commands extracted; cargo test passes.</done>
</task>

## Success Criteria
- [ ] `cargo test --manifest-path src-tauri/Cargo.toml` passes
- [ ] Model, Files, and Diagnostic command IPC functions accessible via `generate_handler!`
