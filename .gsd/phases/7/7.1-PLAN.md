---
phase: 7
plan: 1
wave: 1
---

# Plan 7.1: Extract GPU and Settings Commands

## Objective
Extract GPU discovery and settings management commands from `src-tauri/src/lib.rs` into `commands/gpu.rs` and `commands/settings.rs`, creating `commands/mod.rs`.

## Context
- .gsd/ROADMAP.md
- .gsd/phases/7/RESEARCH.md
- src-tauri/src/lib.rs
- src-tauri/src/sidecar_manager.rs
- src-tauri/src/settings.rs

## Tasks

<task type="auto">
  <name>Extract GPU and Settings command submodules</name>
  <files>src-tauri/src/commands/mod.rs, src-tauri/src/commands/gpu.rs, src-tauri/src/commands/settings.rs, src-tauri/src/lib.rs</files>
  <action>
    1. Create `src-tauri/src/commands/mod.rs` declaring `pub mod gpu;` and `pub mod settings;`.
    2. Move `get_gpus` and `list_gpus` to `src-tauri/src/commands/gpu.rs`.
    3. Move `get_app_settings`, `update_app_settings`, and `get_default_output_dir` to `src-tauri/src/commands/settings.rs`.
    4. Update `src-tauri/src/lib.rs` module declarations and `generate_handler!` macros to reference the command submodules while keeping exact IPC names intact.
  </action>
  <verify>cargo test --manifest-path src-tauri/Cargo.toml</verify>
  <done>GPU and Settings commands extracted; cargo test passes cleanly.</done>
</task>

## Success Criteria
- [ ] `cargo test --manifest-path src-tauri/Cargo.toml` passes with 12 tests
- [ ] Exact IPC command names (`list_gpus`, `get_gpus`, `get_app_settings`, `update_app_settings`, `get_default_output_dir`) are preserved
