---
phase: 9
plan: 2
wave: 2
---

# Plan 9.2: Decompose Video Phases and Encoder Strategy

## Objective
Separate frame extraction, NCNN sequence upscaling, and video reassembly into dedicated phase functions in `src-tauri/src/video_pipeline/phases.rs` and `src-tauri/src/video_pipeline/encoder.rs`.

## Context
- .gsd/ROADMAP.md
- .gsd/phases/9/RESEARCH.md
- src-tauri/src/video_pipeline/mod.rs
- src-tauri/src/video_pipeline/context.rs

## Tasks

<task type="auto">
  <name>Extract video execution phases and encoder fallback strategy</name>
  <files>src-tauri/src/video_pipeline/phases.rs, src-tauri/src/video_pipeline/encoder.rs, src-tauri/src/video_pipeline/mod.rs</files>
  <action>
    1. Create `src-tauri/src/video_pipeline/encoder.rs` defining `EncoderStrategy` enum (`Nvenc`, `Qsv`, `Amf`, `Mf`, `Libx264`, `Mpeg4`) and fallback execution loop.
    2. Create `src-tauri/src/video_pipeline/phases.rs` with `extract_frames`, `upscale_frames`, and `reassemble_video`.
    3. Update `src-tauri/src/video_pipeline/mod.rs` `run_video_job` function to invoke phase functions sequentially.
    4. Run `cargo fmt` and verify full test suite.
  </action>
  <verify>cargo test --manifest-path src-tauri/Cargo.toml && npm.cmd run check:ts</verify>
  <done>Video execution phases decomposed into modular functions; tests passing.</done>
</task>

## Success Criteria
- [ ] `cargo test --manifest-path src-tauri/Cargo.toml` passes
- [ ] `npm.cmd run check:ts` passes
- [ ] Main `run_video_job` function length reduced under 60 lines
