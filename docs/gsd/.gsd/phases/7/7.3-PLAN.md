---
phase: 7
plan: 3
wave: 2
---

# Plan 7.3: Extract Upscale Commands & Refactor Argument Signature

## Objective
Move upscale execution and job cancellation commands into `commands/upscale.rs`. Refactor internal `upscale_image` helper to consume `UpscaleRequest` to resolve `clippy::too_many_arguments`.

## Context
- .gsd/ROADMAP.md
- .gsd/phases/7/RESEARCH.md
- src-tauri/src/lib.rs
- src-tauri/src/job_queue.rs

## Tasks

<task type="auto">
  <name>Extract Upscale commands and refactor internal signature</name>
  <files>src-tauri/src/commands/upscale.rs, src-tauri/src/commands/mod.rs, src-tauri/src/lib.rs</files>
  <action>
    1. Move `run_upscale`, `upscale_image`, `cancel_upscale`, `enqueue_job`, and `cancel_active_job` into `src-tauri/src/commands/upscale.rs`.
    2. Refactor `upscale_image` to accept `UpscaleRequest` struct instead of 9 individual positional arguments.
    3. Update `lib.rs` to compose all command submodules in `tauri::Builder::default().invoke_handler(...)`.
    4. Run `cargo fmt` and verify full test suite.
  </action>
  <verify>cargo test --manifest-path src-tauri/Cargo.toml && npm.cmd run check:ts</verify>
  <done>Upscale commands modularized, parameter count warning resolved, tests passing.</done>
</task>

## Success Criteria
- [ ] `cargo test --manifest-path src-tauri/Cargo.toml` passes
- [ ] `npm.cmd run check:ts` passes
- [ ] No `too_many_arguments` Clippy warnings on upscale handler
