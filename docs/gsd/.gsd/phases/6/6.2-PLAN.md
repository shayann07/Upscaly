---
phase: 6
plan: 2
wave: 1
---

# Plan 6.2: Refactor & Split Titlebar, SettingsPanel, and AdvancedSettings

## Objective
Decompose `Titlebar`, `SettingsPanel`, and `AdvancedSettings` into focused modular sub-components to eliminate function line count and cognitive complexity warnings.

## Context
- .gsd/SPEC.md
- .gsd/phases/6/RESEARCH.md
- src/components/Titlebar.tsx
- src/components/SettingsPanel.tsx
- src/components/AdvancedSettings.tsx

## Tasks

<task type="auto">
  <name>Task 6.2.1: Decompose Titlebar into WindowControls and TitlebarNav</name>
  <files>src/components/Titlebar.tsx, src/components/titlebar/WindowControls.tsx, src/components/titlebar/TitlebarNav.tsx</files>
  <action>
    - Create `src/components/titlebar/WindowControls.tsx` to handle minimize, maximize/unmaximize, and close window IPC calls.
    - Create `src/components/titlebar/TitlebarNav.tsx` for model, history, settings, and about navigation tab buttons.
    - Refactor `Titlebar.tsx` to compose `WindowControls` and `TitlebarNav`.
    - Ensure `Titlebar` function length is under 150 lines and cognitive complexity is under 20.
  </action>
  <verify>npm.cmd run check:ts && npm.cmd run test</verify>
  <done>Titlebar ESLint warnings eliminated cleanly.</done>
</task>

<task type="auto">
  <name>Task 6.2.2: Decompose SettingsPanel and AdvancedSettings</name>
  <files>src/components/SettingsPanel.tsx, src/components/AdvancedSettings.tsx, src/components/settings/GpuSelectorSection.tsx, src/components/settings/OutputFolderSection.tsx, src/components/settings/TunerSection.tsx</files>
  <action>
    - Create `src/components/settings/GpuSelectorSection.tsx` and `OutputFolderSection.tsx` for `SettingsPanel`.
    - Create `src/components/settings/TunerSection.tsx` for `AdvancedSettings`.
    - Refactor `SettingsPanel.tsx` and `AdvancedSettings.tsx` to use extracted sections.
    - Ensure function length is under 150 lines and cognitive complexity is under 20 for both components.
  </action>
  <verify>npm.cmd run check:ts && npm.cmd run test</verify>
  <done>SettingsPanel and AdvancedSettings ESLint warnings eliminated cleanly.</done>
</task>

## Success Criteria
- [ ] `Titlebar.tsx` function length < 150 lines and complexity < 20.
- [ ] `SettingsPanel.tsx` function length < 150 lines and complexity < 20.
- [ ] `AdvancedSettings.tsx` function length < 150 lines and complexity < 20.
- [ ] All unit tests pass cleanly.
