# Plan 9.1 Summary: Extract `VideoJobContext` & Fallible Path Converters

## Overview
Plan 9.1 converted `video_pipeline` into a module directory (`src-tauri/src/video_pipeline/`) and extracted `VideoJobContext` to encapsulate job state, cancellation handles, and scratch directory guards.

## Key Changes
- **`src-tauri/src/video_pipeline/context.rs`**: Created `VideoJobContext` struct and `TempFolderGuard` RAII guard. Replaced panicking `.unwrap()` path string conversions with safe `to_string_lossy()`.
- **`src-tauri/src/video_pipeline/mod.rs`**: Created entry point for the module directory and re-exported `run_video_job`, `resolve_ffmpeg_binary`, and `resolve_ffprobe_binary`.
- **Removed**: Old monolithic `src-tauri/src/video_pipeline.rs`.
