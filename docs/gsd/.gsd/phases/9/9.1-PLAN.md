---
phase: 9
plan: 1
wave: 1
---

# Plan 9.1: Extract `VideoJobContext` and Fallible Path Converters

## Objective
Extract `VideoJobContext` struct into `src-tauri/src/video_pipeline/context.rs` to encapsulate paths, process handles, cancellation state, and temporary scratch directory guards without panicking `.unwrap()` calls.

## Context
- .gsd/ROADMAP.md
- .gsd/phases/9/RESEARCH.md
- src-tauri/src/video_pipeline.rs

## Tasks

<task type="auto">
  <name>Create VideoJobContext and convert video_pipeline to module directory</name>
  <files>src-tauri/src/video_pipeline/mod.rs, src-tauri/src/video_pipeline/context.rs, src-tauri/src/lib.rs</files>
  <action>
    1. Create `src-tauri/src/video_pipeline/context.rs` defining `VideoJobContext` and `TempFolderGuard`.
    2. Add path helper methods on `VideoJobContext` replacing `.to_str().unwrap()` with safe fallible conversions (`to_string_lossy()`).
    3. Initialize `src-tauri/src/video_pipeline/mod.rs` to re-export `run_video_job`, `resolve_ffmpeg_binary`, and `resolve_ffprobe_binary`.
    4. Remove old monolithic `src-tauri/src/video_pipeline.rs`.
  </action>
  <verify>cargo test --manifest-path src-tauri/Cargo.toml</verify>
  <done>VideoJobContext extracted; video_pipeline converted to module directory; cargo test passes.</done>
</task>

## Success Criteria
- [ ] `cargo test --manifest-path src-tauri/Cargo.toml` passes
- [ ] Zero panicking `.to_str().unwrap()` calls in `video_pipeline` module
