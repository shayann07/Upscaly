---
phase: 8
plan: 2
wave: 2
---

# Plan 8.2: Extract Output Paths & Comprehensive Queue Unit Tests

## Objective
Extract output path collision reservation into `src-tauri/src/output_paths.rs`, replace raw mutex unwraps with safe lock handling, and add comprehensive unit tests for queue operations.

## Context
- .gsd/ROADMAP.md
- .gsd/phases/8/RESEARCH.md
- src-tauri/src/job_queue.rs
- src-tauri/src/output_paths.rs

## Tasks

<task type="auto">
  <name>Extract output_paths.rs and add unit tests</name>
  <files>src-tauri/src/output_paths.rs, src-tauri/src/job_queue.rs, src-tauri/src/lib.rs</files>
  <action>
    1. Create `src-tauri/src/output_paths.rs` containing `reserve_output_path` and `release_output_path`.
    2. Add unit tests for `output_paths.rs` verifying path collision reservation for `2x`, `3x`, `4x` scaling factors and index suffix incrementing.
    3. Update `src-tauri/src/job_queue.rs` to use safe mutex lock recovery (`lock().unwrap_or_else(...)`).
    4. Add unit tests for queued job cancellation, active job cancellation, and terminal state idempotency.
    5. Register `output_paths` in `src-tauri/src/lib.rs`.
  </action>
  <verify>cargo test --manifest-path src-tauri/Cargo.toml</verify>
  <done>Output path logic extracted; safe lock handling in place; unit tests passing.</done>
</task>

## Success Criteria
- [ ] `cargo test --manifest-path src-tauri/Cargo.toml` passes with new unit tests
- [ ] No raw `lock().unwrap()` panics on poisoned Mutexes
