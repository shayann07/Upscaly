---
phase: 8
plan: 1
wave: 1
---

# Plan 8.1: Implement `JobState` Enum and `JobQueueService`

## Objective
Create typed `JobState` in `src-tauri/src/job_state.rs` and encapsulate queue state and job controls inside `JobQueueService` in `src-tauri/src/job_queue.rs`.

## Context
- .gsd/ROADMAP.md
- .gsd/phases/8/RESEARCH.md
- src-tauri/src/job_queue.rs

## Tasks

<task type="auto">
  <name>Create JobState enum and encapsulate JobQueueService</name>
  <files>src-tauri/src/job_state.rs, src-tauri/src/job_queue.rs, src-tauri/src/lib.rs</files>
  <action>
    1. Create `src-tauri/src/job_state.rs` defining `JobState` enum (`Queued`, `Running`, `Succeeded`, `Failed(String)`, `Cancelled`).
    2. Add `JobState` methods: `as_str()`, `is_terminal()`, and conversion to/from string status.
    3. Encapsulate queue data structures in `JobQueueService` struct inside `src-tauri/src/job_queue.rs`.
    4. Register `job_state` in `src-tauri/src/lib.rs`.
  </action>
  <verify>cargo test --manifest-path src-tauri/Cargo.toml</verify>
  <done>JobState enum created; JobQueueService encapsulates state; cargo test passes.</done>
</task>

## Success Criteria
- [ ] `cargo test --manifest-path src-tauri/Cargo.toml` passes
- [ ] `JobState` enum replaces string statuses across job queue operations
