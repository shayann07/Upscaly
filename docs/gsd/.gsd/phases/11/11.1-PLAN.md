---
phase: 11
plan: 1
wave: 1
---

# Plan 11.1: Resolve All Residual Clippy Diagnostics

## Objective
Clean up all remaining Clippy warnings in the Rust codebase (`src-tauri/src/**/*.rs`), ensuring `npm.cmd run check:rust` passes cleanly with `-D warnings`.

## Context
- .gsd/ROADMAP.md
- .gsd/phases/11/RESEARCH.md
- src-tauri/src/video_pipeline/phases.rs
- src-tauri/src/video_pipeline/encoder.rs
- src-tauri/src/model_manager.rs
- src-tauri/src/engine/model_store.rs

## Tasks

<task type="auto">
  <name>Resolve residual Clippy warnings in Rust codebase</name>
  <files>src-tauri/src/video_pipeline/phases.rs, src-tauri/src/video_pipeline/encoder.rs, src-tauri/src/model_manager.rs, src-tauri/src/engine/model_store.rs, src-tauri/src/sidecar_manager.rs</files>
  <action>
    1. In `src-tauri/src/video_pipeline/phases.rs`:
       - Decompose `upscale_frames` into `spawn_upscale_engine` and `poll_upscale_progress` to bring function length under 100 lines.
       - Inline format arguments (`{code}`, `{total_frames}`, `{completed}`).
       - Add lock helper to eliminate redundant closure warnings.
       - Add numeric casting annotations (`#[allow(clippy::cast_precision_loss, clippy::cast_possible_truncation)]`) for progress calculations.
    2. In `src-tauri/src/video_pipeline/encoder.rs`:
       - Inline format arguments.
    3. In `src-tauri/src/model_manager.rs` and `src-tauri/src/engine/model_store.rs`:
       - Fix unused variable/function warnings (`_app`, `#[allow(dead_code)]`).
  </action>
  <verify>npm.cmd run check:rust</verify>
  <done>Cargo clippy passes cleanly with -D warnings (0 warnings).</done>
</task>

## Success Criteria
- [ ] `npm.cmd run check:rust` passes with exit code 0
- [ ] `cargo test --manifest-path src-tauri/Cargo.toml` passes
