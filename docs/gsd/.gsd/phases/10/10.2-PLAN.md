---
phase: 10
plan: 2
wave: 2
---

# Plan 10.2: Cache Model Metadata & Optimize Catalog Resolution

## Objective
Implement mtime-based param parsing cache in `src-tauri/src/engine/model_store.rs` to optimize model catalog resolution.

## Context
- .gsd/ROADMAP.md
- .gsd/phases/10/RESEARCH.md
- src-tauri/src/engine/model_store.rs
- src-tauri/src/engine/param_parser.rs

## Tasks

<task type="auto">
  <name>Add param metadata cache in ModelStore</name>
  <files>src-tauri/src/engine/model_store.rs</files>
  <action>
    1. Implement cached `parse_ncnn_param_cached(path: &Path) -> Result<ModelMetadata, String>` in `src-tauri/src/engine/model_store.rs`.
    2. Maintain `PARAM_CACHE` (`Mutex<HashMap<PathBuf, (SystemTime, ModelMetadata)>>`).
    3. Update `resolve_catalog` and `determine_model_status` to use cached param metadata resolution.
    4. Verify full test suite.
  </action>
  <verify>cargo test --manifest-path src-tauri/Cargo.toml && npm.cmd run check:ts</verify>
  <done>Param metadata caching added; catalog resolution optimized; tests passing.</done>
</task>

## Success Criteria
- [ ] `cargo test --manifest-path src-tauri/Cargo.toml` passes
- [ ] `npm.cmd run check:ts` passes
- [ ] Param parsing reuses cached result if file mtime is unchanged
